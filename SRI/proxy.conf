<VirtualHost *:80>

      ServerName tdpn.ddns.net 

      ProxyPreserveHost On
      RequestHeader set X-Forwarded-Proto "http"
      RequestHeader set X-Forwarded-Port "80"

      #<Proxy "balancer://servidores">
      #      BalancerMember "https://10.0.1.30:80/"
      #      BalancerMember "https://10.0.1.82:80/"
      #      ProxySet lbmethod=byrequests
      #</Proxy>

      ProxyPass "/" "http://10.0.1.30/"
      ProxyPassReverse "/" "http://10.0.1.30/"
          
      ErrorLog ${APACHE_LOG_DIR}/proxy-ssl-error.log
      CustomLog ${APACHE_LOG_DIR}/proxy-ssl-access.log combined
</VirtualHost>
